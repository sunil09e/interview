Terraform Module
A Terraform module is a reusable collection of Terraform code.
Think of it like a function in programming.
Instead of writing the same EC2/VPC code again and again, you write it once inside a module and use it whenever you need it.
We use a Terraform module mainly to reuse the same Terraform code instead of writing it again and again.

A module is not a separate Terraform language. It is simply a folder containing .tf files that you call using:
module "name" {
  source = "./modules/ec2"
}

And the values go into the module through variables:
Root module
   ↓
inputs (variables)
   ↓
Child module
   ↓
resources
   ↓
AWS

The Terraform configuration you currently have (main.tf, provider.tf, variable.tf, data.tf, etc.) is itself the root module. 
When you create modules/ec2, that becomes a child module.


Without module

Suppose you need 3 VPCs:

dev VPC
prod VPC
test VPC

Without a module, you may write the VPC resources 3 times:

resource "aws_vpc" "dev" {
  cidr_block = "10.0.0.0/16"
}

resource "aws_vpc" "prod" {
  cidr_block = "10.1.0.0/16"
}

resource "aws_vpc" "test" {
  cidr_block = "10.2.0.0/16"
}

This creates duplicate code.

With a module

You write the VPC code once:

modules/
└── vpc/
    ├── main.tf
    ├── variables.tf
    └── outputs.tf

Then reuse it:

module "dev_vpc" {
  source     = "./modules/vpc"
  name       = "dev"
  cidr_block = "10.0.0.0/16"
}

module "prod_vpc" {
  source     = "./modules/vpc"
  name       = "prod"
  cidr_block = "10.1.0.0/16"
}

module "test_vpc" {
  source     = "./modules/vpc"
  name       = "test"
  cidr_block = "10.2.0.0/16"
}


----------------------------------

vpc ec2 we need for 2 zones measn the structure and the code

If you want VPC + EC2 in 2 Availability Zones, a common Terraform structure is:

terraform-project/
│
├── providers.tf
├── main.tf
├── variables.tf
├── outputs.tf
│
└── modules/
    ├── vpc/
    │   ├── main.tf
    │   ├── variables.tf
    │   └── outputs.tf
    │
    └── ec2/
        ├── main.tf
        ├── variables.tf
        └── outputs.tf

The idea is:
              VPC
             10.0.0.0/16
                    │
        ┌───────────┴───────────┐
        │                       │
       AZ-1                    AZ-2
        │                       │
   Public Subnet           Public Subnet
   10.0.1.0/24             10.0.2.0/24
        │                       │
      EC2-1                   EC2-2


1. providers.tf

This is in the root module.

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
  }
}

provider "aws" {
  region = "ap-south-1"
}


2.Root main.tf
Now we call both modules.

module "vpc" {
  source = "./modules/vpc"
}

module "ec2" {
  source = "./modules/ec2"

  vpc_id        = module.vpc.vpc_id
  subnet_ids    = module.vpc.subnet_ids
  ami           = "ami-xxxxxxxxxxxxxxxxx"
  instance_type = "t3.micro"
}

The important part is this:

vpc_id     = module.vpc.vpc_id
subnet_ids = module.vpc.subnet_ids

The VPC module creates the VPC/subnets, then gives their IDs through outputs.
The EC2 module receives those IDs and creates the EC2 instances.


3.Root outputs.tf
output "vpc_id" {
  value = module.vpc.vpc_id
}

output "instance_ids" {
  value = module.ec2.instance_ids
}

output "public_ips" {
  value = module.ec2.public_ips
}

If there is only 1 EC2
Better to call it:
output "instance_id" {
  value = aws_instance.web.id
}

This gives:
instance_id = "i-123456789"
If there are multiple EC2s

Use:
output "instance_ids" {
  value = aws_instance.web[*].id
}


If you want the output to be a single ID instead of a list, you could use:
output "instance_id" {
  value = aws_instance.web[0].id
}


VPC module
2. modules/vpc/main.tf
data "aws_availability_zones" "available" {
  state = "available"
}

resource "aws_vpc" "main" {
  cidr_block           = "10.0.0.0/16"
  enable_dns_hostnames = true
  enable_dns_support   = true

  tags = {
    Name = "my-vpc"
  }
}

resource "aws_internet_gateway" "igw" {
  vpc_id = aws_vpc.main.id

  tags = {
    Name = "my-igw"
  }
}

resource "aws_subnet" "public_1" {
  vpc_id                  = aws_vpc.main.id
  cidr_block              = "10.0.1.0/24"
  availability_zone       = data.aws_availability_zones.available.names[0]
  map_public_ip_on_launch = true

  tags = {
    Name = "public-subnet-1"
  }
}

resource "aws_subnet" "public_2" {
  vpc_id                  = aws_vpc.main.id
  cidr_block              = "10.0.2.0/24"
  availability_zone       = data.aws_availability_zones.available.names[1]
  map_public_ip_on_launch = true

  tags = {
    Name = "public-subnet-2"
  }
}

resource "aws_route_table" "public" {
  vpc_id = aws_vpc.main.id

  route {
    cidr_block = "0.0.0.0/0"
    gateway_id = aws_internet_gateway.igw.id
  }

  tags = {
    Name = "public-route-table"
  }
}

resource "aws_route_table_association" "public_1" {
  subnet_id      = aws_subnet.public_1.id
  route_table_id = aws_route_table.public.id
}

resource "aws_route_table_association" "public_2" {
  subnet_id      = aws_subnet.public_2.id
  route_table_id = aws_route_table.public.id
}

4. modules/vpc/outputs.tf
output "subnet_ids" {
  value = [
    aws_subnet.public_1.id,
    aws_subnet.public_2.id
  ]
}

output "vpc_id" {
  value = aws_vpc.main.id
}

EC2 module
5. modules/ec2/main.tf
resource "aws_security_group" "ec2" {
  name   = "ec2-sg"
  vpc_id = var.vpc_id

  ingress {
    description = "SSH"
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  tags = {
    Name = "ec2-sg"
  }
}

resource "aws_instance" "web" {
  count = 2

  ami           = var.ami
  instance_type = var.instance_type
  subnet_id     = var.subnet_ids[count.index]

  vpc_security_group_ids = [
    aws_security_group.ec2.id
  ]

  tags = {
    Name = "web-${count.index + 1}"
  }
}

Here:

count = 2

means Terraform creates 2 EC2 instances.
And:

subnet_id = var.subnet_ids[count.index]

means:

EC2-1 → subnet-1 → AZ-1

EC2-2 → subnet-2 → AZ-2


6. modules/ec2/variables.tf
variable "vpc_id" {
  type = string
}

variable "subnet_ids" {
  type = list(string)
}

variable "ami" {
  type = string
}

variable "instance_type" {
  type = string
}

the values comes from root main
Root main.tf provides the values
module "ec2" {
  source = "./modules/ec2"

  vpc_id        = module.vpc.vpc_id
  subnet_ids    = module.vpc.subnet_ids
  ami           = "ami-123456"
  instance_type = "t3.micro"

7. modules/ec2/outputs.tf
output "instance_ids" {
  value = aws_instance.web[*].id
}

output "public_ips" {
  value = aws_instance.web[*].public_ip
}


What happens when you run it?
terraform init
terraform plan
terraform apply

Terraform creates:

VPC
10.0.0.0/16
│
├── Internet Gateway
│
├── Public Subnet 1
│   10.0.1.0/24
│   AZ-1
│      │
│      └── EC2-1
│
└── Public Subnet 2
    10.0.2.0/24
    AZ-2
       │
       └── EC2-2

--------------------------------
It asks AWS:

"What Availability Zones are available in ap-south-1?"

AWS might return:

names[0] → ap-south-1a
names[1] → ap-south-1b
names[2] → ap-south-1c

availability_zone = data.aws_availability_zones.available.names[0]

Suppose AWS gave:

names[0] = ap-south-1a
names[1] = ap-south-1b



resource "aws_instance" "web" {
  count = 2

  ami = var.ami

  instance_type = var.instance_types[count.index]

  subnet_id = var.subnet_ids[count.index]

  tags = {
    Name = "web-${count.index + 1}"
  }
}

Now this becomes very easy.

First EC2

Because:

count = 2

Terraform starts:

count.index = 0

Then:

var.instance_types[0]

gives:

t2.micro

And:

var.subnet_ids[0]

gives:

subnet-111111

That subnet is in AZ-1.

Therefore:

EC2-1
│
├── Type: t2.micro
├── Subnet: subnet-111111
└── AZ: ap-south-1a
Step 13 — Second EC2

Terraform then uses:

count.index = 1

So:

var.instance_types[1]

gives:

t3.micro

And:

var.subnet_ids[1]

gives:

subnet-222222

That subnet is in AZ-2.

Therefore:

EC2-2
│
├── Type: t3.micro
├── Subnet: subnet-222222
└── AZ: ap-south-1b


If you wanted 2 subnets in only 1 AZ

You would use [0] for both:

resource "aws_subnet" "public_1" {
  vpc_id            = aws_vpc.main.id
  cidr_block        = "10.0.1.0/24"
  availability_zone = data.aws_availability_zones.available.names[0]
}

resource "aws_subnet" "public_2" {
  vpc_id            = aws_vpc.main.id
  cidr_block        = "10.0.2.0/24"
  availability_zone = data.aws_availability_zones.available.names[0]
}
-----------------------------------------

Yes. If you mean:

AZ-1 → EC2 with t2.micro
AZ-2 → EC2 with t3.micro

Structure
VPC
│
├── AZ-1
│    └── Subnet-1
│         └── EC2-1 → t2.micro
│
└── AZ-2
     └── Subnet-2
          └── EC2-2 → t3.micro
EC2 module main.tf
resource "aws_instance" "web" {
  count = 2

  ami = var.ami

  instance_type = var.instance_types[count.index]

  subnet_id = var.subnet_ids[count.index]

  vpc_security_group_ids = [
    aws_security_group.ec2.id
  ]

  tags = {
    Name = "web-${count.index + 1}"
  }
}
variables.tf
variable "ami" {
  type = string
}

variable "instance_types" {
  type = list(string)
}

variable "subnet_ids" {
  type = list(string)
}

variable "vpc_id" {
  type = string
}
Root main.tf
module "vpc" {
  source = "./modules/vpc"
}

module "ec2" {
  source = "./modules/ec2"

  vpc_id     = module.vpc.vpc_id
  subnet_ids = module.vpc.subnet_ids

  ami = "ami-xxxxxxxxxxxxxxxxx"

  instance_types = [
    "t2.micro",
    "t3.micro"
  ]
}

Terraform matches them by index:

instance_types[0] = t2.micro
subnet_ids[0]     = AZ-1
                    ↓
                  EC2-1


instance_types[1] = t3.micro
subnet_ids[1]     = AZ-2
                    ↓
                  EC2-2

So you can have different instance types in different AZs.
---------------------------------
Why doesn't the first one need alias?

Because Terraform already treats the first configuration as the default.

So this:

provider "aws" {
  region = "ap-south-1"
}

can simply be referred to as:

aws

The second one needs a name:

provider "aws" {
  alias  = "us_east"
  region = "us-east-1"
}

and is referred to as:

aws.us_east
---------------------------------
Let's build a 2-region Terraform module structure step by step.

We will create:

Region 1 → ap-south-1 → EC2 t2.micro
Region 2 → us-east-1   → EC2 t3.micro

Each region gets its own VPC.

Step 1: Folder structure

Create this:

terraform-project/
│
├── providers.tf
├── main.tf
├── outputs.tf
│
└── modules/
    ├── vpc/
    │   ├── main.tf
    │   ├── variables.tf
    │   └── outputs.tf
    │
    └── ec2/
        ├── main.tf
        ├── variables.tf
        └── outputs.tf


Step 2: providers.tf

This is where we define the two regions.

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
  }
}

provider "aws" {
  region = "ap-south-1"
}

provider "aws" {
  alias  = "us_east"
  region = "us-east-1"
}
Now we have:

aws
 ├── default → ap-south-1
 └── us_east → us-east-1


root main.tf


# =========================
# REGION 1 - MUMBAI
# =========================

module "vpc_mumbai" {
  source = "./modules/vpc"

  vpc_cidr = "10.0.0.0/16"
  vpc_name = "mumbai-vpc"
}

module "ec2_mumbai" {
  source = "./modules/ec2"

  vpc_id        = module.vpc_mumbai.vpc_id
  ami           = "ami-xxxxxxxx"
  instance_type = "t2.micro"
}


# =========================
# REGION 2 - US EAST
# =========================

module "vpc_us" {
  source = "./modules/vpc"

  providers = {
    aws = aws.us_east
  }

  vpc_cidr = "10.1.0.0/16"
  vpc_name = "us-vpc"
}

module "ec2_us" {
  source = "./modules/ec2"

  providers = {
    aws = aws.us_east
  }

  vpc_id        = module.vpc_us.vpc_id
  ami           = "ami-yyyyyyyy"
  instance_type = "t3.micro"
}


Step 4: VPC module main.tf
resource "aws_vpc" "this" {
  cidr_block = var.vpc_cidr

  tags = {
    Name = var.vpc_name
  }
}

Step 3: VPC module
modules/vpc/variables.tf
variable "vpc_cidr" {
  type = string
}

variable "vpc_name" {
  type = string
}

The module is saying:

"Give me a VPC CIDR and a VPC name."


Step 5: VPC module outputs.tf
output "vpc_id" {
  value = aws_vpc.this.id
}

When AWS creates the VPC, AWS gives it an ID:
vpc-123456789
The output sends that ID back to the root.



Step 7: EC2 module main.tf

For simplicity, create a subnet and EC2 inside the same module:

resource "aws_subnet" "this" {
  vpc_id     = var.vpc_id
  cidr_block = "10.0.1.0/24"

  tags = {
    Name = "ec2-subnet"
  }
}

resource "aws_instance" "this" {
  ami           = var.ami
  instance_type = var.instance_type
  subnet_id     = aws_subnet.this.id

  tags = {
    Name = "web-server"
  }
}

Notice:
subnet_id = aws_subnet.this.id
Terraform creates the subnet first, gets its ID, then puts the EC2 into that subnet.

Step 6: EC2 module variables.tf
variable "vpc_id" {
  type = string
}

variable "ami" {
  type = string
}

variable "instance_type" {
  type = string
}

The EC2 module needs:

vpc_id
ami
instance_type



Step 8: EC2 module outputs.tf
output "instance_id" {
  value = aws_instance.this.id
}

output "subnet_id" {
  value = aws_subnet.this.id
}


Step 11: Final structure

Now everything looks like this:

                         ROOT
                          │
              ┌───────────┴───────────┐
              │                       │
          ap-south-1              us-east-1
              │                       │
              ↓                       ↓
        VPC MODULE                VPC MODULE
              │                       │
         Mumbai VPC                 US VPC
              │                       │
              ↓                       ↓
        EC2 MODULE                EC2 MODULE
              │                       │
          t2.micro                 t3.micro

------------------------------------
The module's main.tf does NOT decide the region.
The provider decides the region.

Think of it like this:

MODULE = What to create
PROVIDER = Where to create it
1. Module main.tf

You write the resource only:

resource "aws_vpc" "this" {
  cidr_block = var.vpc_cidr

  tags = {
    Name = var.vpc_name
  }
}

This says:

"Create an AWS VPC."

It does not say Mumbai or US.

2. Provider says where

In root providers.tf:

provider "aws" {
  region = "ap-south-1"
}

provider "aws" {
  alias  = "us_east"
  region = "us-east-1"
}

Now Terraform has:

aws
 ↓
ap-south-1

aws.us_east
 ↓
us-east-1
3. Root calls the same module for Mumbai
module "vpc_mumbai" {
  source = "./modules/vpc"

  vpc_cidr = "10.0.0.0/16"
  vpc_name = "mumbai-vpc"
}

Because we didn't specify a provider, it uses the default:

vpc_mumbai
     ↓
default aws
     ↓
ap-south-1
     ↓
VPC created in Mumbai
4. Root calls the SAME module for US
module "vpc_us" {
  source = "./modules/vpc"

  providers = {
    aws = aws.us_east
  }

  vpc_cidr = "10.1.0.0/16"
  vpc_name = "us-vpc"
}

This part:

providers = {
  aws = aws.us_east
}

means:

"For this module, use the aws.us_east provider."

So:

vpc_us
   ↓
aws.us_east
   ↓
us-east-1
   ↓
VPC created in US
So the same main.tf is reused
modules/vpc/main.tf
        │
        │ same code
        │
   ┌────┴────┐
   ↓         ↓
Mumbai      US
   ↓         ↓
aws        aws.us_east
   ↓         ↓
ap-south-1 us-east-1

Simple rule to remember:

Module main.tf tells Terraform WHAT resource to create.
Provider tells Terraform WHERE to create it.

for ec2

Yes. EC2 works exactly the same way.

EC2 module main.tf = what EC2 to create
Provider = which region to create it in

1. EC2 module main.tf

Inside:

modules/ec2/main.tf

you write:

resource "aws_instance" "this" {
  ami           = var.ami
  instance_type = var.instance_type
  subnet_id     = var.subnet_id

  tags = {
    Name = var.name
  }
}

Notice: there is no region here.

2. Root provider
provider "aws" {
  region = "ap-south-1"
}

provider "aws" {
  alias  = "us_east"
  region = "us-east-1"
}

So:

aws          → Mumbai
aws.us_east  → US
3. Create EC2 in Mumbai

Root main.tf:

module "ec2_mumbai" {
  source = "./modules/ec2"

  ami           = "ami-mumbai"
  instance_type = "t2.micro"
  subnet_id     = "subnet-mumbai"
  name          = "mumbai-ec2"
}

No provider specified.

Therefore:

ec2_mumbai
    ↓
default aws
    ↓
ap-south-1
    ↓
EC2 created in Mumbai
4. Create EC2 in US

Use the same EC2 module again:

module "ec2_us" {
  source = "./modules/ec2"

  providers = {
    aws = aws.us_east
  }

  ami           = "ami-us"
  instance_type = "t3.micro"
  subnet_id     = "subnet-us"
  name          = "us-ec2"
}

Now:

ec2_us
   ↓
aws.us_east
   ↓
us-east-1
   ↓
EC2 created in US
The whole idea
             modules/ec2/main.tf
                     │
              SAME EC2 CODE
                /          \
               /            \
              ↓              ↓
      ec2_mumbai          ec2_us
           ↓                  ↓
         aws              aws.us_east
           ↓                  ↓
     ap-south-1           us-east-1
           ↓                  ↓
       EC2-1                EC2-2

So you don't write Mumbai/US inside the EC2 module's main.tf.

You reuse the same module and choose the region from the root using the provider.
