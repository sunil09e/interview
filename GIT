Your computer   Local branch
GitHub          Remote branch
origin/test → remote branch
origin → name of the GitHub repository connection
origin/test
   ↑    ↑
 remote branch

A branch is not a separate empty folder.
When you create a branch:
git switch -c dev
it starts from the current branch's files.


Ancestor in Git means the earlier commit from which the current branches came.
Suppose initially you have:
file.txt
Hello World
You commit it: Commit 1
Then you create two branches:
Commit 1 is the common ancestor of main and dev.
-------------------------------------------------------------------------------------

git --version      Check Git version
git config --global user.name "Your Name"    Set username
git config --global user.email "you@example.com"   set email
git config --list       See configuration
git config -l
git config --list --show-origin      See the complete configuration

git init       Makes the current folder a Git repository.
git clone <repository-url>       Clone an existing repository  
git clone https://github.com/user/project.git    Downloads the repository to your computer.
git status         Shows: Modified files,Untracked files,Staged files,Current branch,Commit status
git restore --staged <file>..."    to unstage

git add file1.txt file2.txt   Add multiple files
git add file1.txt    Moves the file to the staging area.
git add .      Add everything

It is used to select which changes you want to include in the next commit
Git tracking → Git keeps track of changes/history.
Staging area → You choose which changes go into the next commit.
Commit → Permanently records those staged changes in Git history.

git commit -m "Add login feature"


git log       to check the commit  Shows commit history.
git log --stat  Detailed log   See only the names of files changed by each commit
git log --oneline  shows what are the commit msg and commit id
git log --oneline -- file1.txt   See commits for a particular file
git log -- file1.txt    See all commits that changed a particular file
git log -- project/
git log -p -- file1.txt   See the actual changes made to that file  -p means show the actual changes/diff.
git log -p -- project/
git log -1 -- file1.txt   See only the latest commit that changed the file
git blame file1.txt    See who changed each line
git diff abc123 def456 -- file1.txt    See changes between two commits for a particular file
git diff abc123 def456 -- project/


git blame file1.txt   Find who changed each line

git show    Shows details of the latest commit.
git show    means show the details of a commit.
git show 1e9cb0e:file1.txt         show the content of the file
git show 1e9cb0e    This shows the details of commit abc123.
git show HEAD:file1.txt
git reflog  we can see all commit

git diff  See unstaged changes    file are already staged but now you change some code but not push to the staged
           Shows changes you made but haven't staged.
git diff --staged  See staged changes    Shows changes that have been added with git add.
git diff abc123 def456 -- file1.txt    See changes between two commits for a particular file
git diff abc123 def456 -- project/
git diff abc123 def456    shows differences between two commits.

git branch --show-current     Find which branch you're on
git branch -r           See remote branches         You might see: origin/main  origin/dev
git branch -a             See all branches
git branch                See branches   to check what are the branches are avaliable
git branch -vv             see current branch
git branch development    Create branch
git switch development    Switch branch
git switch -c development   Create and switch
git checkout -b development  Older/common syntax:  to switch   or git checkout dev
git branch -d development    Delete branch
git branch -D development    Force delete
git branch -m dev             If your current branch is test and you want to rename it to dev   if you want to change first go to that branch then change
    -m → rename branch, but fails if the new name already exists.
     -M → force rename branch, even if the new branch name already exists.
    If test is already pushed to GitHub
    You also need to rename the remote branch:
git branch -m dev
git push -u origin dev
git push origin --delete test
   So:
    git branch -m dev → rename local branch
    git push -u origin dev → push new dev branch
    git push origin --delete test → delete old test branch from remote.


git remote -v    See remote
git remote add origin https://github.com/user/project.git    Add remote
git remote set-url origin <new-url>    Change remote URL
git remote remove origin   Remove remote
git remote add upstream https://github.com/company/project.git   upstream is a name you can give any name  add one more

git clone    Used when you don't have the repository locally yet.
git clone https://github.com/user/project.git
git fetch    Used when you already have a Git repository locally and want to get newer information/commits from a remote.


git rm file1.txt    Delete file and stage deletion
git rm --cached file1.txt    Delete file from Git but keep it locally
dont do at the same time it can completly delete
--cached means remove from Git's index but keep the physical file. It doesn't make sense after you've already removed the file with git rm.

git commit --amend                means modify your most recent commit instead of creating another separate commit.
You made a commit: git add file1.txt  git commit -m "Add login"
Then you realize you forgot to include file2.txt.

Instead of creating another commit:
git add file2.txt
git commit -m "Add file2"
you can add it to the previous commit:
git add file2.txt
git commit --amend

git commit --amend -m "Add login feature"  You can also change the commit message
This changes the last commit's message.

Important
--amend works on the latest commit only.



git mv old.txt new.txt   Rename Files

touch .gitignore   Create  .gitignore

git clean -n  Delete untracked files:
git clean -f   ⚠️ Be careful. It permanently removes untracked files.

git switch -c development origin/development      Create a new local branch called development based on the remote-tracking branch origin/development, and switch to it.
-c → create a new branch  origin/development → use the remote-tracking branch as the starting point
Suppose GitHub has:origin/main
origin/development

But your local computer only has:main

You want to work on the remote development branch locally.
Run: git switch -c development origin/development
Now you have:
Remote:  origin/development
       ↓
Local:
development
And you are automatically switched to:
* development
  main


Git Tags
Tag = human-friendly name/label for an important commit
A Git tag is a name/label that points to a specific commit.
Think of it like putting a label on an important version.
Create a tag
Suppose you are currently at: commit3  git tag v1.0
create a tag  git tag v1.0
git tag   See tags
git tag v1.0 <C2-commit-id>   Tag an older commit
git show v1.0  It shows the commit information and changes.
git tag -d v1.0    This removes the tag locally.
git push origin v1.0  Push a tag to GitHub
git push origin --tags      push all tags

Annotated tag
There are two common types.
Lightweight tag
git tag v1.0
Simple pointer to a commit.

Annotated tag
git tag -a v1.0 -m "Version 1.0 release"
This stores extra information such as: tag name,message,tagger,date
For releases, annotated tags are commonly preferred.



restore
--------------
Suppose your history is:
Commit 1 → file1.txt = Hello
Commit 2 → file1.txt = Welcome
Commit 3 → file1.txt = Good Morning  you are here
git restore --source=<commit-1-id> file1.txt
  If you want to make that restored version part of your branch history:
git add file1.txt 
git commit -m "Restore file1 from Commit 1"
----------------
git restore --source=first id  file1.txt  If you want to completely go back to Commit 1
git reset --hard commit id     If you want your entire repository to become exactly like Commit 1:
-------------

git revert     is for committed changes
git restore file1.txt    not in staged and commited we change hello to sunil  now i think i need previous cotent hello so we use this but hello is already in staged 
git restore file1.txt   Deleted but not committed but in staged                  No — in this exact situation, git restore file1.txt 
               cannot bring back Hello if Hello was never staged or committed.
git restore --staged file1.txt  = "Remove this file from the staging area, but keep my changes."
git restore --source=<commit-1-id> file1.txt   current is commit2  Git takes file1.txt from Commit 1 and puts it into your working directory.
                                               But Commit 2 is still your current commit.
Instead of deleting the commit, use:
git revert <commit-2-id>        undo 2 and go back to commit1
For example:
git revert abc1234
Git creates a new Commit 3        revert created a commit, but restore not create acommit
Example:

Commit 1 → Hello
     ↓
Commit 2 → World
     ↓
git revert Commit-2
     ↓
Commit 3 → Hello

git restore --staged file1.txt   This means unstage the file.
Example You modify file1.txt:  Hello → Welcome
Then: git add file1.txt
Now the file is staged and ready for commit.
If you change your mind and don't want it staged yet:
git restore --staged file1.txt
Now:
Staging area:
file1.txt ❌ removed from staging
Working directory:
file1.txt → Welcome ✅

git restore file1.txt         This works when the deletion is not committed.  you deleted but you only staged not commited 
What if you already committed the deletion?
Suppose you did:
rm file1.txt
git add file1.txt
git commit -m "Delete file1"

Now the deletion is part of Git history.
In that case, git restore file1.txt by itself won't restore the old version because the current HEAD says the file is deleted.

You can restore it from the previous commit, for example:
git restore --source=HEAD~1 file1.txt
Then commit the restored file:
git add file1.txt
git commit -m "Restore file1"

Deleted but not committed → git restore file1.txt
Deleted and committed → git restore --source=<older-commit> file1.txt

reset
------
git reset --soft HEAD~9  = "Go back 9 commits, but keep all the work from those 9 commits staged so I can make one clean commit."
git reset --soft HEAD~2      if the 3 commit msg is worng i need to change the commit msg so give head2 to remove the commit3 msg but the content in staged  then  give commit msg it will commit 
HEAD means: Current commit = Commit 3
HEAD~1 means: One commit before HEAD = Commit 2
HEAD~2 means: Two commits before HEAD = Commit 1
Commit 1 remains. Commits 2–10 are removed from the branch history, their changes are combined and staged, and you create one new commit containing those changes.

Why use --soft?A common use is when you made a commit too early or with the wrong commit message, and you want to undo the commit while keeping all its changes staged.
For example:git commit -m "wrong message"
Then: git reset --soft HEAD~1
Now the commit is removed from the branch, but your changes are still staged, so you can make a new commit:

git commit -m "correct message"
Remember
git reset --soft HEAD~1
HEAD:
Commit 2 → removed from branch history
Changes:
Commit 2's changes → STAGED ✅

cat > sunil.txt   sunil
git add .
git commit -m "first"
now i changed
cat > sunil.txt world
git add .
git commit -m "second"
git log --oneline sunil.txt
          77f066d (HEAD -> master) second
           f39f04b first
 git reset --soft HEAD~1
 git log --oneline sunil.txt
        f39f04b (HEAD -> master) first
git show HEAD:sunil.txt
sunil
 git commit -m "New Commit"
 git log --oneline sunil.txt
536759c (HEAD -> master) New Commit
f39f04b first
 git show HEAD:sunil.txt
world
 git show HEAD~1:sunil.txt
sunil
git reflog  we can see all commit
So you can still access them.
For example: git show 67b71ba
shows the old third commit.

git reset --soft HEAD~1
        ↓
Commit removed
Changes → STAGED

git reset HEAD~1
        ↓
Commit removed
Changes → UNSTAGED

git reset --hard HEAD~1
        ↓
Commit removed
Changes → DELETED

git reset --hard HEAD~1
Commit 1 → file1.txt = Hello
Commit 2 → file1.txt = World
                         ↑
                        HEAD
if i do git reset --hard HEAD~1 menas  hello will there and commit 2 will deleted and content also commit one will be there
Yes. Exactly. ✅
Commit 2 immediately destroyed?   ❌ No, usually recoverable through git reflog

git reset --hard commit id        If you want your entire repository to become exactly like Commit 1:
git reset --hard <commit-1-id>    your branch points to Commit 1, and your entire working project is made to match Commit 1.
Commit 1 → Commit 2 → Commit 3
   ↑           ↑           ↑
 Hello       World     Good Morning
                         HEAD
After:  git reset --hard <commit-1-id>
you get:
Commit 1
   ↑
  HEAD
What happens to 2 and 3?  They are no longer part of the current branch history.
So when you run: git log --oneline
you'll normally see: Commit 1

And your whole working project is restored to how it was at Commit 1:
Files = exactly the state at Commit 1
Content = exactly the state at Commit 1

However, saying "Commit 2 and 3 are permanently deleted" is not quite correct. They may still be recoverable using:
git reflog
So the best wording is:
git reset --hard <commit-1-id> makes Commit 1 the current commit and makes the entire working project match Commit 1. Commits 2 and 3 are removed from the
current branch history, but may still be recoverable through the reflog.


revert
-----------
git revert is used when you want to undo a previous commit without deleting the commit history.
   you can revert both a previous commit and the current/latest commit.
Suppose your Git history is:
Commit 1 → Commit 2 → Commit 3   commit1 is sunil 2 is sunilkumar 3 is sunilkumar123
now i git revert <commit3 id>    undo 3 and go to 2 commit msg has a commit id 4
if you see git log you can see commit4  is Revert "third"  third is commit msg of commit3
if you see cat file.txt means you can see the commit2  (sunilkumar)content has a commit4 msg

if you  git revert <commit1 id>  means it will delete the file   reverting Commit 1 means Git tries to remove file.txt.
so you need to git revert --abort         
if you  git revert <commit2 id>  commit msg is  Revert "second" commitid is now 5  content is commit1 (sunil)

But that is NOT what revert means.
git revert C2 means: "Undo exactly what C2 did."
What did C2 do?
C2 changed: Sunil → Sunilkumar
So Git needs to reverse that change: Sunilkumar → Sunil



Suppose we have one file called file.txt.
Commit 1  file.txt Sunil
Commit 2 You change the same file:
file.txt Sunilkumar
Commit 3
You change it again:
file.txt Sunilkumar123

So history is:C1 → C2 → C3
and currently you are at: C3
with: Sunilkumar123

Now you say: "I want C1 content"
You might think: git revert C2
means: C3 → C1

But that is NOT what revert means.
git revert C2 means: "Undo exactly what C2 did."

What did C2 do?
C2 changed: Sunil → Sunilkumar
So Git needs to reverse that change: Sunilkumar → Sunil

But where are you currently?
You are at C3:Sunilkumar123
So Git sees: What C2 changed: Sunil → Sunilkumar
Reverse of C2:
Sunilkumar → Sunil

Current content:Sunilkumar123
Git says:"Wait! I expected Sunilkumar, but the current file has Sunilkumar123."
Why? Because C3 changed the same line after C2.
Therefore Git may say: CONFLICT

git revert --skip   skip that return back to current
git revert --continue  conflict made the continue means both cotents come then edit and add and commit
git revert --abort  to cancel the revert
then 
git add file1.txt
git revert --continue  both content will come we decide which one we need edit manually  then ad  commit
 if you add . means we cant skip or abort 
or git revert --abort  

If you want C1 content temporarily
You can use: git switch --detach <C1-id>
If you want to permanently move your branch from C3 back to C1
Use:git reset --hard <C1-id>


Example
C1 = Sunil
C2 = Sunilkumar
C3 = Sunilkumar123

Run:git revert C3 C2
Git creates two new commits:
C1 → C2 → C3 → Revert C3 → Revert C2
Final file.txt: Sunil
git revert C2 C3   you may encounter a conflict if C3 modified the same content that C2 modified.


Stash
-----------
git stash temporarily saves uncommitted changes.

git stash    Save changes
git stash pop   Apply and remove stash

git stash list  See stashes  Example: stash@{0} stash@{1}
git stash apply   Apply latest stash
git stash apply stash@{0}   Apply a specific stash
git stash drop stash@{0}   Delete a stash
git stash clear   Delete all stashes

git stash is used when you have uncommitted changes in your working directory, but you don't want to commit them yet.
It temporarily saves your changes and gives you a clean working directory.

Simple example
You are working on: file.txt
Original committed content: Hello
You modify it:Hello Welcome

But you haven't committed it: git status
You see: modified: file.txt

Now you suddenly need to switch to another task/branch.
Instead of committing unfinished work, use: git stash
Git temporarily stores your changes.

Your file goes back to the last committed version: Hello

And: git status
shows a clean working tree.

Where did your changes go?
They are stored in the stash.

Check: git stash list

Example: stash@{0}: WIP on main
Your changes are not deleted.

Get your changes back
Use: git stash pop

git stash apply   Restores the changes but keeps the stash.
Stash
 ↓
Apply changes
 ↓
Stash still exists

git stash pop    Restores the changes and removes the stash if it applies successfully.
Stash
 ↓
Apply changes
 ↓
Stash removed

Multiple stashes
You can stash multiple times:
git stash
git stash
git stash

Check them: git stash list
Example:
stash@{0}  ← newest
stash@{1}
stash@{2}  ← oldest
To apply a specific one: git stash apply stash@{1}




push
----------------
git push origin main    Push branch    Local main to GitHub main
git push -u origin main   -u means set the upstream branch.
This does two things:
     Pushes your local main to origin (GitHub).
     Connects your local main branch with the remote origin/main branch.
     "Remember that these two branches are connected."
So next time: git push
  Git already knows:  "Push the current local main to origin/main."
git switch master
git push -u origin master
Now Git remembers:
    local main    → origin/main
    local master  → origin/master
So later: git switch main  git push
pushes to:  origin/main
and:
git switch master    git push
pushes to: origin/master

pull     fetch + merge
-------------------
git switch main
git pull
knows to pull from: origin/main
You don't need to write: git pull origin main
conflict during pull?
GitHub has: file.txt Hello Kumar
Your local main has: file.txt Hello Sunil
Both changed the same part.
You run:
git pull
Git may produce:
<<<<<<< HEAD
Hello Sunil
=======
Hello Kumar
>>>>>>> origin/main
That's a merge conflict.
You manually choose the final content.
Then: git add file.txt
git commit         That's enough for a normal merge.
or, depending on the operation/state:
git merge --continue

if i changed but not commited the file1 but same file1 in github iam try to pull what will happen
error: Your local changes to the following files would be overwritten by merge:
    file1.txt
Please commit your changes or stash them before you merge.
If you have:
Local: file1.txt → uncommitted change
GitHub: file1.txt → changed
git pull    Git will usually stop before merging if the pull would overwrite your uncommitted local changes.
Then you can either:
git add file1.txt
git commit -m "My changes"
git pull     Git will likely get a merge conflict:
or:
git stash     Git temporarily stores your change and makes your working directory look like the last committed version:
git pull
git stash pop   <<<<<<< Updated upstream
Hello Kumar
=======
Hello Sunil
>>>>>>> Stashed changes
Now you decide what the final content should be
Then: git add file1.txt
git commit -m "Resolve stash conflict"
git commit → permanently records change in Git history
git stash  → temporarily puts change aside
suppose local has file1 and 2 hithub has fil3 and 4 the pull means main has now 4 files
Yes, exactly, assuming the files are committed on both sides and there are no conflicts.


fetch
--------
normally git fetch origin fetches updates for all branches that the origin remote is configured to track, not just main.
git fetch downloads the remote commits into your local Git repository, but it does not put the files into your current working folder.
Suppose GitHub has: file1.txt file2.txt file3.txt file4.txt
Your local main has: file1.txt file2.txt
You run: git fetch origin
Now Git knows about file3.txt and file4.txt, but your ls may still show only:
file1.txt
file2.txt

git log main..origin/main --oneline   See commits that are on GitHub but not local
git diff main origin/main        This shows exactly what changed.
git ls-tree -r --name-only origin/main   See the files in origin/main
git merge origin/main   now the file are came
main..origin/main   Now you can see what commits are available on GitHub before merging them.  for inspect:
This means: Show me the commits that exist in origin/main but do NOT exist in my local main.
git log main..origin/main --oneline
Output:
Commit 4
Commit 3
This tells you: "GitHub has Commit 3 and Commit 4, but my local main doesn't have them."
Why use fetch instead of pull?
Because sometimes you don't want Git to immediately merge changes.
Instead of: git pull
you can first do:git fetch
Then inspect:git log main..origin/main
or: git diff main origin/main
You can see what changed.
Then decide: git merge origin/main

git fetch origin main   To fetch a specific branch:

if a local file1 has sunil remote has file1 kumar then fetch means conflict or what?
Local main:
Sunil
origin/main:
Kumar
You run: git fetch origin
When you try to merge the two:git merge origin/main
If both changed the same part of file1.txt, Git may give a conflict:

we fetch all from remote now i switch to master the merge the master means the master code will come 
if iam in main then i merge the master means the master code will come in main?
Yes, exactly. But one small correction: git merge master means bring the changes from master into the branch you are currently on.
It doesn't simply replace your branch with all of master's files.

if local file1 not commited from remote the file1 is fetch what will happen?
Then if you try:  git merge origin/main
Git sees that your local file has an uncommitted change.
If merging the remote change would overwrite that local change, Git will usually stop with something like: error
If you want to keep your local change:

git add file1.txt
git commit -m "Save local change"
git merge origin/main

Or temporarily put it aside:
git stash
git merge origin/main
git stash pop

If you don't want your local change:
git restore file1.txt     then your uncommitted changes are removed, and the file returns to the last committed version of your current branch.
So:Hello Sunil  ❌ uncommitted change is removed
   Hello World  ✅ previous committed content comes back
git merge origin/main

the file has hello already i changed in the remote to world if i fetch and merge means
Ah, now I understand. You mean the original file already had Hello, and you changed that file to World only on the remote (GitHub). You did not change the local file.
Then yes — after fetch + merge, the local file becomes World. ✅

git fetch checks the configured remote branches and downloads only the new data/commits you don't already have.
It does not download the entire branches again, and it does not merge the changes.
git fetch    Git uses the configured/default fetch settings of your repository
Example:origin
├── main
├── dev
└── master
git fetch    → Fetches the configured branches from the configured remote(s).   use only you have one remote origin   Download remote branch information

git fetch origin    Fetch from the remote named origin.    for multiple like project1 and 2
git fetch origin  → Fetches the configured branches from origin.
git remote add upstream https://github.com/company/project.git   upstream is a name you can give any name
git fetch --all    → fetch from both remotes.
git remote rename upstream company
git config --list --show-origin      See the complete configuration

Suppose you don't want origin to fetch all branches. You want only main.
git config --unset-all remote.origin.fetch
git config --add remote.origin.fetch +refs/heads/main:refs/remotes/origin/main
git fetch origin   will fetch only the configured main refspec.
git config --get-all remote.origin.fetch
You should see:

+refs/heads/main:refs/remotes/origin/main



git merge --continue    If you want to finish the merge
git merge --abort      If you want to cancel the merge

merge
----------------
there are 2 branches master and main
master has  file1.txt file2.txt
main has same 
content change from dev not in main
if you merge the content and what are the file in there it will come to main

Fast-forward merge 
------------------
You create dev from main.
if dev has file1.txt file2.txt file3.txt
master has file1.txt file2.txt
if you merge dev to master file3.txt will added  in master
One-line interview answer
“Fast-forward merge means Git moves the current branch pointer forward to the other branch's latest commit without creating a merge commit.”
If interviewer asks "Why no merge commit?"
Say: “Because there is no divergent history. The target branch is directly behind the source branch, so Git doesn't need to combine two separate histories.”

Before creating dev  main has 3 files
Now you create dev from main.
main has file1.txt file2.txt file3.txt
dev has file1.txt file2.txt file3.txt and you added file4.txt  deleted file2.txt file3.txt  without git add and commit 
file2.txt and file3.txt not in the main but we can restore
so main now has 1 and 4
so if you use add and commit now the main has all 4 files  and dev has 1 and 4
That deletion belongs to that branch.

Both branches originally have:
file1.txt → HelloWorld
file2.txt → Sunil
main changes file1.txtfile1.txt → Welcome
dev changes file2.txt file2.txt → Kumar
git switch main git merge dev
So the result on main is:
file1.txt → Welcome
file2.txt → Kumar
so both file is change at the time then merge means conflict
so change the file in any one branch then merge

main: file1.txt file2.txt file3.txt
You create dev from an earlier commit, before file2 and file3 were added:
dev: file1.txt Then you add: file4.txt
git switch main git merge dev
main will normally contain: all 4 files

Three-way merge
--------------
When dev was created, both branches had:
file.txt
Hello World
This is the common ancestor.
main changes the file
dev also changes the file
Now merge You want dev changes in main
Git looks at three versions:
1. Common ancestor → Hello World
2. main            → Hello Sunil
3. dev             → Hello Kumar
Git cannot decide automatically.
So you get:
<<<<<<< HEAD
Hello Sunil
=======
Hello Kumar
>>>>>>> dev
This is a merge conflict.
You need to remove the conflict markers and decide which content you want to keep.
Yes, manually means you open the conflicted file and change its content yourself.
if it is a merge conflict, use:
git merge --abort

step1: Fix the conflict manually
You edit the file and decide:Hello Kumar
Step 2: Tell Git the conflict is resolved
git add file.txt
Step 3: Continue the merge
git merge --continue
This tells Git:"I have resolved the conflicts. Continue and finish the merge."

why we use continue after add ?
------------------
Because after a merge conflict, git add only tells Git that you fixed the file. It doesn't necessarily finish the merge by itself.
git merge dev
Conflict occurs.
You fix file1.txt, then:git add file1.txt
Now Git knows:"The conflict in file1.txt has been resolved."
But the merge operation is still in progress.
So:git merge --continue
tells Git:"I have resolved all conflicts. Now finish the merge."
But there is an important detail
For a normal merge, after:
git add file1.txt
you can usually simply do:
git commit
Git creates the merge commit.

So these are commonly equivalent ways to finish a conflict-resolved merge:
git add file1.txt
git commit                 That's enough for a normal merge.
or, where supported:
git add file1.txt
git merge --continue        git merge --continue is mainly useful for operations such as rebase or cherry-pick, where Git explicitly pauses and asks you to continue.









Command	Purpose
git init	Create a Git repository
git status	Check repository status
git add .	Stage all changes    Now the file moves into the staging area.
git commit -m "message"	Save changes as a commit
git log	View commit history
git branch	View branches
git switch	Switch branches
git checkout	Older command for switching/restoring
git clone	Download a GitHub repository
git remote -v	Show remote repository
git push	Upload commits to remote
git pull	Download and integrate changes
git fetch	Download remote changes without integrating
git merge	Combine branches
git config user.name
git config user.email

git show    Shows details of the latest commit.
git show    means show the details of a commit.
git show 1e9cb0e:file1.txt         show the content of the file
git show 1e9cb0e    This shows the details of commit abc123.


if there is 3 commits now iam in the 3 commit i need the first commit content so what to do
git log --oneline
git restore --source=first or second id  file1.txt
If you want to completely go back to Commit 1
If you want your entire repository to become exactly like Commit 1:
git reset --hard commit id


git revert = create a new commit that undoes an earlier commit.
git revert --abort    First cancel the current failed revert:
git revert 659312c5318a2cbec1d6611cf5d78e1d596a5001c    If you want to undo the third commit, you can do it from your current location:
git restore --source=<commit-3-id> file1.txt   This restores the file content from Commit 3.
git revert <commit-4-id>     "Keep Commit 4 in history, but create a new commit that undoes Commit 4." undo 4 and go back to commit3
git reset --hard <commit-3-id>         "Move my project and HEAD back to Commit 3.  You want your whole project to move back to Commit 3
reset --hard = move the branch pointer backward and remove V4 from the visible branch history.
revert = keep V4 and create a new commit that undoes V4.
But important: V4 is not immediately destroyed
Git may still have the V4 commit internally for some time, and you can often recover it using:
git reflog
For example:
HEAD@{0} → V3
HEAD@{1} → V4
Then you can recover V4 by using its commit ID:
git reset --hard <V4-commit-id>

git log --stat  Detailed log
git log --oneline  shows what are the commit msg and commit id
 git log -- file1.txt    See all commits that changed a particular file
git log -- project/
git log -p -- file1.txt   See the actual changes made to that file  -p means show the actual changes/diff.
git log -p -- project/
git log -1 -- file1.txt   See only the latest commit that changed the file
git blame file1.txt    See who changed each line
git diff abc123 def456 -- file1.txt    See changes between two commits for a particular file
git diff abc123 def456 -- project/



Staging area means a temporary area where you select the changes that you want to include in the next commit.
A git commit is a saved snapshot of your project's changes in a Git repository.
Git repository means a folder/project that Git is tracking.
├── .git/        ← Git repository data
The .git folder stores things like:
Commit history
Branches
Commit IDs
Information about changes
Git created a commit.

Git automatically generated a unique commit ID/hash:

6594dc20fec385cfbc5ddc9a9...


GIT


What is Git?

Git is a version control system (VCS).

It is software installed on your computer that tracks changes in your source code.

For example, you have:

project/
 ├── app.py
 ├── index.html
 └── style.css

You modify app.py several times.

Git allows you to keep track of those changes:

Version 1 → Version 2 → Version 3 → Version 4

If something goes wrong, you can go back to an earlier version.

2. What is GitHub?

GitHub is a cloud-based platform that hosts Git repositories.

Think of it like this:

Git
 ↓
Manages your code and versions
 ↓
Your local computer

GitHub
 ↓
Stores/shares your Git repository online
 ↓
Cloud/server

So:

Git = tool/software for version control
GitHub = online platform for storing and collaborating on Git repositories

What does git revert do?

It undoes the changes made by a particular commit, but it does not delete that commit from Git history.
Commit 1 created this file, so reverting it means deleting it
First cancel the current failed revert:

git revert --abort
It only undoes the changes made by Commit 3.
Commit 1 = sunil
Commit 2 = sunil kumar
Commit 3 = sunil kumar world

revert Commit 3
       ↓

Result = sunil kumar
